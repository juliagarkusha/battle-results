import React, { FC } from "react";
import "./ErrorMessage.scss";

const ErrorMessage: FC = () => {
  return (
      <div className="ErrorMessage">Error! Please try again</div>
  );
};

export default ErrorMessage;
